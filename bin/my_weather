#!/usr/bin/env python

import sys
from my_weather import weather

if len(sys.argv) == 2:
    weather_code = sys.argv[1]
    weather.main(weather_code)
else:
    print('enter 1 city code from weather.gc.ca')
